namespace Alca.MonoGame.Kernel.Platform;

/// <summary>Represents the platform type the game is running on.</summary>
public enum PlatformType
{
    /// <summary>Desktop operating system (Windows, Linux, macOS).</summary>
    Desktop,

    /// <summary>Mobile operating system (Android, iOS).</summary>
    Mobile,

    /// <summary>Console platform (Xbox).</summary>
    Console
}
