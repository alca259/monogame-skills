namespace Alca.MonoGame.Kernel.Input;

public enum MouseButton
{
    /// <summary>Left mouse button.</summary>
    Left,
    /// <summary>Right mouse button.</summary>
    Right,
    /// <summary>Middle mouse button.</summary>
    Middle,
    /// <summary>First extra mouse button (often used for back navigation).</summary>
    XButton1,
    /// <summary>Second extra mouse button (often used for forward navigation).</summary>
    XButton2
}
